# Force native modules to compile against Electron's Node.js headers.
# This ensures better-sqlite3 (and any future native deps) target
# the correct ABI even during initial npm install compilation.
runtime = electron
disturl = https://electronjs.org/headers
