# Native module compilation for Electron is handled by the postinstall script:
#   npx electron-rebuild -w better-sqlite3
# Do NOT add blanket runtime=electron / disturl overrides here —
# that breaks host-Node build tools (macos-alias, fs-xattr, etc.)
# used by Electron Forge makers.
