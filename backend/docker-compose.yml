services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: operation_nexus
      POSTGRES_USER: nexus
      POSTGRES_PASSWORD: nexus_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
